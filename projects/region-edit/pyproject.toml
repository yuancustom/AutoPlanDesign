[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autoplan-region-edit"
version = "0.1.0"
requires-python = ">=3.11"
description = "AutoPlanDesign P2: development scaffold, not production implementation"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
