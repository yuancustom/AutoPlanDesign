# 原型验证报告

日期：2026-09-11。范围：单文件浏览器原型，不是后端、求解器或模型验收。

## 实际执行

`node --test tests/core.test.cjs`：22项通过，0失败。检查简单多边形/自交/退化、受限直线路径解析、整数数量/余数分配、层数与总量、SHA-256与Node标准实现一致、网络禁用声明。

`python tests/browser_test.py --chromium /usr/bin/chromium --out <临时输出目录>`：13个场景通过、0失败、页面运行错误0。实际测试尺度确认阻止跳步、等比校准、SVG导入、危险/不支持输入拒绝、拖点/撤销/重做、闭合绘图、25柜分配、非整数错误、快照失效、固定示意标志、SVG下载、PNG导出、390px初始页面横向溢出。

测试环境为当前Linux Chromium，通过Playwright set_content装载HTML（零网络）。受管理浏览器不允许file/http导航，未改变该策略，故没有声称完成本机HTTP导航或Mac/Safari测试。实际SVG/PNG导出已执行，不是生成模型的图片。

localStorage处于不允许持久化的渲染器源，已显示会话草稿提示；刷新恢复持久化未测。网页源文件仍支持普通浏览器中的尽力持久化，但不得作为审计证据或唯一备份。

## 未执行

真实模型推理、正式API、布局求解器、任意SVG完整语义、跨层独立轮廓、P2/P3交接、BIMFACE、工程审查、用户可用性验收：全部NOT_RUN或NOT_IMPLEMENTED。

结果页图像是固定合成示意；没有虚构模型分数或耗时。测试通过不能将其改为正式方案。
