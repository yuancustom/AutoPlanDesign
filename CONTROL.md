# AutoPlanDesign 开发总控文档

版本：2.0.0-planning｜日期：2026-09-11｜维护分支：main｜产品负责人：仓库所有者

**此文档管控三个独立项目及最终 Agent 的开发。当前交付是治理与接口骨架，不是全部功能上线。**任务状态以 `governance/backlog.json` 为准；模型实验以逐次运行记录为准；证据不足不晋级。

## 1. 产品目标与拆分决策

最终工作流：带尺寸闭合轮廓 → 初步平面方案 → 用户涂抹/文字局部修改 → 确认平面版本 → 三维模型和二维图纸。

使用单仓库多项目，而非本轮新建三个远端仓库。各项目有独立 pyproject、源码、测试、CLI 入口和验收；集成层只调用公开接口。将来迁仓不应改变文件协议。这里的“合并”是能力编排，不是三套代码再次耦合。

| ID / 项目 | 输入 | 输出 | 不负责 |
|---|---|---|---|
| P1 outline-plan | 带真实尺寸、单位与证据的闭合轮廓，楼层与功能要求 | 每层平面图片、几何侧车、尺度变换、候选版本与校验记录 | 区域交互编辑、最终CAD/三维导出 |
| P2 region-edit | 指定平面版本、涂抹蒙版、文字指令、修改类型 | 修改候选、前后对比、逐步事件、几何更新或过期待重建标记 | 无审批改外壳/其他楼层；输出工程合规结论 |
| P3 plan-delivery | 用户确认的最终平面版本及匹配几何、层高与必要参数 | 同源生成 DXF、PDF、GLB | 重做平面设计；擅自统一轮廓或移动楼梯 |
| Agent 集成层 | 用户任务、项目状态、资源与审批 | 三项目调度、模型路由、进度、失败恢复、结果交付 | 跳过项目校验与人工审批 |

## 2. 当前基线，禁止夸大

P1：旧提交的 L 形示意工具链和47项历史测试保留在 baseline。旧数据是 diagram_units，不是新需求的真实尺寸输入；布局是显式案例，不是任意轮廓自动排房。ComfyUI 接口存在，不代表 Mac 已完成推理。

P2：本轮建立独立项目和修改协议，尚未实现涂抹界面、局部模型编辑或长期审计存储。

P3：已找到用户的 `floorplan-to-dxf-pdf-glb` v1.0.0 完整 Skill 包；本次仓库导入中 `assets/project.schema.json`、`scripts/validate_project.py`、`scripts/validate_deliverables.py` 三个文件的写入被平台拦截；其余16个文件冻结保存。导入状态为PARTIAL_BLOCKED，不宣称已完整集成。它包含指令、模板及输入/文件检查脚本，不含通用识图模型或完整建模引擎。项目必须先适配，不得声称“已有 Skill=自动重建已实现”。

新项目包仅提供能力探测接口；模型运行、评分、导出统一标为 NOT_RUN。先证明单项目，再逐步连接，最后才叫完整 Agent。

## 3. 全局硬约束

### 3.1 尺度与坐标

建筑世界坐标统一米，X/Y平面、Z向上。各层相同原点与方向；图片坐标原点左上，u向右、v向下。保存可逆 `pixel_to_world` 变换及源图尺寸；显示缩放和图板排版不得写回世界坐标。

闭合外环与孔洞分别记录，显式首尾闭合；校验自交、退化、孔洞包含与相交。优先使用可信矢量几何/标注；一条可信长度仅能在无拉伸/透视的相似变换假设下定尺度。轮廓只有几条边长未必唯一确定形状，仍需顶点/角度/轴网等证据。

每个尺寸保存数值、单位、证据、来源分类及确认状态。总宽总长矛盾、图像非等比拉伸、层高未知必须 BLOCKED 或请求确认，不按常见值补成事实。真实尺度未知不能进入米制交付。

旧 L 形没有用户确认的米制尺寸。合同 fixture 的米制 L 形为 SYNTHETIC 测试数据，不用于冒充用户实际方案。

### 3.2 图片、几何与版本

项目间传 `PlanRevision`，不只传一个PNG路径。至少含 project/building/floor IDs、revision与parent、图像哈希、几何哈希及状态、坐标变换、尺寸事实、校验与审批引用。

P2 改了布局后，图片与几何必须生成同一新版本；若尚不能同步重建，geometry_status=STALE 并阻断 P3。纯视觉修改也须检查是否误画假墙/假门。外部图片没有侧车时走显式 IMAGE_ONLY 识图校准路径，不能猜测后继续。

### 3.3 本地、隐私与安全

默认模型、图片、历史都留本机。Mac mini 16GB 是首个测试档案，芯片/系统/后端版本需实机记录；batch=1、concurrency=1，切模型前卸载。更大模型在用户批准的其他硬件上比较，默认不等于允许云端上传。

不提交权重、字体、凭据、个人图纸或本机绝对路径。公共仓库仅用合成或明确授权的脱敏数据。模型生成的代码不得直接执行；Skill工具固定白名单，提示词不能改权限或审批。

### 3.4 能力状态

区分 CONTRACT_ONLY、GEOMETRY_BASELINE、MOCK、DRY_RUN、MODEL_EXECUTED、HUMAN_ACCEPTED、NOT_RUN、BLOCKED。调用成功不等于质量通过；文件可读不等于工程合规；后处理覆盖墙线不证明原始模型没改形。

## 4. P1：尺寸轮廓到初步布置

### 路线

P1-01 输入校准与闭合轮廓检查 → P1-02 功能程序与多楼层几何提案 → P1-03 受控图面生成 → P1-04 模型对比与人工验收。

保留“先几何再表现”的基线，同时测试 LLM/VLM 提案的作用；独立评价布局质量与图片表现。空间不足、核心放不下、上下层不衔接时报告冲突，不悄悄改外壳。三层可用 per_floor 边界，不因旧示例同轮廓而强制相同。

### 最小可用验收

能输入一栋有明确单位的 L 形，配置1~N层和每层功能；保存尺度证据；产生不同功能的各层图及匹配几何；外壳/孔洞不改变；房间/设备不越界；必需功能存在；门连接成立；跨层固定核心对齐；原始模型图片和程序版都留存。

边界精度与算法容差分别记录：示意图 IoU 不能代替尺寸检查；从像素估出的尺寸不能因小数位多变成实测。门宽/消防/荷载等仅在明确条件与专业检查后给结论。

### 测试覆盖

凸/凹/曲线/有孔洞轮廓、窄颈、冲突尺寸、自交、分层退台、极小可用面积、入口/核心冲突；合成金标准与真实脱敏图分开。先 L 形贯穿试点，再扩展，不一次铺开20形状。

## 5. P2：区域修改和全过程记录

### 用户操作流程

选择建筑/楼层/源版本 → 在原图上涂抹或擦除 → 输入指令 → 显示允许修改区与受保护区 → 生成候选 → 对比 → 用户接受/拒绝 → 新修订。

画布至少计划支持画笔大小、缩放/平移、撤销/重做和蒙版导出。记录画布CSS尺寸、设备像素比、缩放平移矩阵及回投到原图的变换，解决“屏幕涂对、原图改错”的问题。蒙版白=可编辑、黑=保护，alpha单独记录；有效修改区与提供给模型的上下文区不是一回事。

### 两种修改权限

appearance：设备示意、房名或图面表达，不能移动实体边界。layout：增删房间、移动门墙、调整核心，必须生成几何补丁和拓扑复核。改变外壳/楼梯/跨层服务核时列出受影响楼层，单独审批，不因用户涂了附近区域就默认允许全楼修改。

### 每次尝试必须记录

| 类别 | 必须留存 |
|---|---|
| 来源 | before版本/PNG/hash、所选楼层、base_revision、操作者与时间 |
| 区域 | 原始笔画、原分辨率蒙版、有效修改区、保护区、坐标变换 |
| 指令 | 用户原文、结构化意图、公开的简短决策摘要、实际模型提示词 |
| 执行 | 模型/量化/权重hash、后端、参数、种子、workflow、工具调用、耗时、错误与重试 |
| 结果 | raw_after、final_after、差异图、几何补丁、校验、接受/拒绝原因 |
| 谱系 | 新revision、parent、请求幂等键、审批绑定、下游失效事件 |

“过程”是可审计的输入、工具步骤、参数、公开决策摘要与结果，不要求模型隐藏思维链。失败候选和拒绝版本也保留；不能只保存好看的结果。

### 数据完整性

采用不可变版本＋追加事件。旧文件不可覆盖；撤销/回滚生成新版本并指向来源；候选分叉是业务版本，不需要Git分支。base_revision 做乐观并发检查，来源变了返回 CONFLICT；相同幂等键不重复付费调用。请求日志先持久化再执行，崩溃可恢复，取消/超时留记录。

事件hash链只提供完整性校验，不等于防管理员篡改。部署时还需访问控制、备份及保留策略；用户数据删除须单独授权并区分引用失效与物理擦除。

### 验收

可完成至少三轮涂抹修改、一次拒绝、一次回滚、一次并发冲突、一次崩溃恢复。每个版本可重建公开过程，before/after完整。无损最终PNG在有效修改区以外像素差为0；同时评价原始模型泄漏，不用裁切掩盖模型问题。真实布局变化后P3不能继续使用旧几何。

## 6. P3：复用现有 Skill 的重建与交付

已允许写入的原始合同与工具保存在 `projects/plan-delivery/vendor/floorplan-to-dxf-pdf-glb/`，来源、hash和被拦截的缺失文件见 IMPORT.json。完整集成验收前必须补齐IMPORT.json中登记的三个原始文件；本轮不重试被拦截写入，也不编码绕过。原检查脚本不可运行，P3骨架检查通过不等于Skill集成通过。适配器放 vendor 之外，原工具升级必须更新清单和回归，不直接改原版事实口径。

P3-01 平面版本/几何/参数适配 → P3-02 VLM识图与BuildingIR → P3-03 构件化重建和同源导出 → P3-04 回读与看图验收。

接入优先利用已批准几何，但独立项目必须能处理只有图片的输入。后者需要识图、尺度、层高、对齐和置信度复核，不把幻觉几何当输入。禁止只把图片贴到平板上或把外轮廓拉高就宣称完成建筑重建。

建筑交付目录仅含一份多层 DXF（毫米/模型空间1:1）、一份多页矢量 PDF、一份自包含 GLB（米/Y向上）。说明、假设、修改记录、未完成项写入PDF及相应元数据；日志/PNG/JSON留在内部工作区，不额外交付STEP/OBJ/HTML等。

三格式从同一个 BuildingIR 导出，稳定 floor/object IDs 贯穿。建筑Z向上到GLB转换只做一次；合图排版偏移不能进入三维。门窗要开洞、楼梯要有层间连接/楼板开洞；层高变化重算楼梯，不复制台阶。

入口检查阻止 STALE/IMAGE_ONLY未经重建/未批准版本。交付前回读三格式检查单位、尺度、楼层、标高、洞口及对象一致性；PDF逐页渲染检查，GLB实际查看；未运行项目NOT_RUN。修改尺寸、层高、批准策略后，三种成果整体失效并重生成。

## 7. 共用接口与最终 Agent

合同版本见 contracts/README.md。当前用文件JSON＋内容hash交接；未来HTTP或队列只换传输，不改变语义。项目返回结构化错误：NEEDS_INPUT、CONFLICT、GEOMETRY_INVALID、RESOURCE_EXCEEDED、MODEL_FAILED、REVIEW_REQUIRED；不得用空文件当成功。

Agent 状态机见 agent/states.json：intake → scale_confirmed → plan_candidate → plan_approved → edit_candidate（可循环）→ final_plan_approved → reconstruction → export_review → delivered。任何阶段可 blocked/cancelled。用户不需要修改时，允许显式确认P1版本为最终版本，不能自动跳过最终审批。

原审批只对被确认revision/hash和范围有效。新修改使已有重建/导出STALE；暂停恢复从最后持久检查点继续。模型路由依据实际基准、能力兼容、资源与授权，不能只按“参数最多”或宣传成绩选。高风险改形与导出有独立人工关卡。

## 8. 模型分层、实验和选择

规范在 benchmarks/PROTOCOL.md，候选清单在 models.json。将“推理/视觉理解层”和“图像生成/编辑层”分别比较，不能把4B语言模型和4B图像模型当同一种资源等级。

T0=确定性/人工金标准；T1=16GB优先试的小模型组合；T2=中档或更高质量候选，需要实测内存；T3=大模型参考线，用户批准更大本地硬件后执行。不是所有层次都强塞进16GB，也不自动调用云端。

P1评尺度/外壳/拓扑/功能/图面；P2评指令遵循、区内效果、区外泄漏、几何同步、历史完整性；P3评对象识别、尺寸/单位/开洞/层间关系、跨格式一致性；Agent评任务成功率、人工介入、恢复、时间/成本/本地隐私。

同案例、同输入版本、同任务和测试预算，逐条保存全部尝试。协议校验或黄金几何不通过的候选先淘汰，不能靠美观平均分抵消。硬件组分别排行，不混合Mac与大GPU延时。对比须有原图和最终图两个视图。

## 9. 里程碑与验收关卡

| 里程碑 | 交付与关卡 | 放行条件 |
|---|---|---|
| M0 治理基线 | 三项目目录、总控、合同v0.1、模型注册、任务台账、CI | 治理与独立骨架测试通过；不代表模型实现 |
| M1 纵向最小样板 | P1带尺寸L形；P2确定性编辑和日志；P3输入适配 | 单案例完整版本链，mock明确标识 |
| M2 真实单项目模型 | P1/P2/P3分别跑T1，记录性能和失败 | 每项目模型原始结果＋人工审查，不用CI代替 |
| M3 横向模型比较 | T0/T1/T2；T3按授权硬件 | 固定开发集/留出集、完整原始记录及选择报告 |
| M4 Agent连接 | 路由、暂停/审批、重试/幂等、失效传播 | L形三轮改图再三格式交付，含失败恢复回归 |
| M5 扩展与发布 | 其他形状/分层不同轮廓、版本迁移、性能预算 | 冻结版本与发布验收；不自动宣称工程合规 |

不给未经实机测量的小时/天承诺。每阶段实际工时与阻塞写台账。允许按已知合同并行：P2用合成PlanRevision启动；P3用已确认fixture做适配，无需等P1模型选完。

## 10. 开发过程控制

每条任务写ID、项目、负责人角色、依赖、验收、证据、状态和阻塞。状态：TODO/READY/IN_PROGRESS/BLOCKED/REVIEW/DONE/DEFERRED。DONE须有证据和评审；更改合同或验收阈值必须附变更说明，不可先看分数再改门槛。

每次开发：认领任务 → 阅读输入合同/基线 → 写方案与测试 → 实现 → 单项目测试 → 合同回归 → 留证据 → 更新台账 → 提交main → 回读CI。提交粒度小、携带任务ID，禁止强推与用fixture伪造模型实测。

每次交接写清：本轮改了什么、运行了什么命令/设备、结果在哪、未做哪些、下一任务及阻塞。模板见 governance/templates/。讨论中的想法先进入ADR/需求，不默默改变已批准图纸。

一次影响跨项目的变更应更新合同版本、变换/单位迁移、消费方测试和旧版本兼容策略；不允许只在P2升级字段让P3静默误读。

## 11. 风险登记与停止条件

| 风险 | 对策 / 停止条件 |
|---|---|
| 米制误判、透视或非等比拉伸 | 保存证据与变换；尺度矛盾先停 |
| 修改图与几何不同步 | 版本/hash绑定；STALE阻止P3 |
| 区域之外漂移 | 原始泄漏评分＋最终无损保护；不能只看覆盖图 |
| 上下层核心断裂 | 跨层影响分析＋单独审批/回归 |
| 16GB内存不足 | 串行、量化/分辨率对照实验；不关闭保护或云fallback |
| 模型看似正确但不可编辑 | 基于BuildingIR导出与回读；图片或mesh外观不够 |
| 基准被挑图/数据泄漏污染 | 所有尝试归档；开发/留出分组冻结；失败也入分母 |
| 旧样板被当成新系统 | 基线冻结并标示diagram_units；新能力单独验收 |
| 自动记录被当成真实授权 | 用户界面确认＋操作者/版本/事件证据；禁止Agent自批 |

## 12. 本轮清理与交付范围

旧根的 scripts/configs/examples/prompts/skills/tests/docs/evidence 从主入口移走，保留完整旧树在 P1/baseline，历史提交不重写。根目录只放开发总控、共享协议、实验治理、集成设计与CI。

既有P3 Skill已导入文件保持原字节，被平台拦截的Schema及两个检查脚本作为明确缺项登记；默认H形50×30/层高4/3/3示例仍为样例，不推广为新项目事实。工具包文档不属于P3建筑成果，故可以存在仓库，但实际deliverables仍仅三格式。

本轮不承诺已完成涂抹UI、尺寸通用排房、真实模型对比、完整三维自动引擎或端到端Agent。上述能力有明确任务、验证路径和停止条件，下一轮从 READY 任务开始。
