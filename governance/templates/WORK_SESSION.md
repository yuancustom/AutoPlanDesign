# 开工与交接卡｜<task_id> / <session_id>

模板状态：TEMPLATE_ONLY。复制到对应项目 `handoffs/<session_id>.md`，不要把本模板填写为真实运行证据。共享治理任务写入 `governance/handoffs/`。每轮新建文件，不覆盖上一轮。

## A. 开工登记（先交总控确认）

- project / task_id / parent_task_id：
- session_id / actor_id / coordinator：
- base_main_sha / started_at / next_checkpoint：
- 合同版本与文件 hash：
- 已读文件及关键约束：
- 允许修改路径 / 明确排除路径：
- 输入来源、尺寸确认状态、是否允许公开：
- 依赖状态与证据；阻塞父任务下的独立准备范围：
- 本轮最小目标与正常/失败验收用例：
- 模型是否需要运行、硬件/显存内存预算和设备申请：
- 协调登记引用 / 认领确认事件 / expires_at：

## B. 实施与可复现证据

| 变更 | 路径 | 原因 | 上下游影响 |
|---|---|---|---|
| 待填 | 待填 | 待填 | 待填 |

| 命令与输入/配置 hash | 执行平台/依赖版本 | 退出码 | 产物/日志位置 | 证据类型 |
|---|---|---|---|---|
| 未执行 | 未登记 | null | 无 | NOT_RUN |

分别记录正常用例、失败用例和回归。模型实验引用 benchmarks 记录和私有 raw/final 制品，不复制敏感图纸到公共 Git。不要把没跑的指标写为0或PASS。

## C. 交接状态（分维度，不用总百分比）

- 文档：
- 原型：
- 生产实现：
- 模型实测：NOT_RUN（只有真实执行证据才修改）
- 用户验收：PENDING（填实际反馈引用，不自行批准）
- 尚未实现/尚未测试：
- 接口提案与迁移/回退：
- 阻塞、处理角色、解阻条件：
- 下一任务建议及依赖：

## D. 发布与关闭（总控补充）

- queue_id / 当前认领是否有效：
- 发布前 main / 变化文件白名单 / 最新基础回归：
- 实际提交 SHA / 回读结果：
- CI run 与结论（失败不得省略）：
- 工程评审意见 / 是否达到该任务 DONE 条件：
- backlog 与 delivery_status 的更新位置：
- 认领/设备释放记录：

发布是代码动作，不代表用户方案已批准。用户数据恢复不等于 Git revert；迁移失败要明确数据恢复措施。
