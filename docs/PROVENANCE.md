# L 形来源与规范化

原始图来自用户在本对话提供的 20 种建筑轮廓总览，选取第 02 个 L 形，原裁切画布为 307×214。

发布图 examples/source_crops/02.png **不是彩色原截图**。处理规则是对 RGB 像素选择 `(B-R)>20 and (G-R)>10 and R>60`，选中的像素统一为 [170,215,245]，其他像素为白色。保留原画布及每个选中像素的位置，不按“L 形”名称重新画形状。

原裁切 SHA-256：`185e83462b830121e36816dbfa545881cd942bb1656e07cfa8cc74d85f1906d4`。

发布二色图 SHA-256：`1ce06270338ee5c1db62bfdc427c9d6a9b3e487fe04df1aef4ee3940e9c7531d`。

处理前后蓝色选区逐像素相同；详细记录见 `examples/source_crops/PROVENANCE.json`。低分辨率输入的小折点仍保留，不宣称恢复了原始 SVG。原始彩色截图保存在此前交付的对话 ZIP 中，本仓库不把规范化图冒充原图。

本例单位是 diagram_units，没有真实尺度或北向。三层外边界相同是显式任务假设；楼梯 A/B、卫生间位置是候选布局，不是工程批准结果。模型原图必须另行检查，几何 IoU 不能当成模型质量分数。
